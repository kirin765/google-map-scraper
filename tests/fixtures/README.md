# Test fixtures

This directory stores HTML snapshots and other stable inputs used by parser-oriented tests.

Fixtures should be small, deterministic, and representative of the fields the parsers need to extract.
