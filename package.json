{
  "name": "google-map-scraper",
  "version": "0.1.0",
  "private": true,
  "description": "TypeScript scraper for Google Maps place, review, and review-photo extraction.",
  "type": "module",
  "main": "./dist/src/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rm -rf dist",
    "start": "node dist/src/index.js",
    "test": "npm run build && node --test $(find dist/tests -name '*.test.js' -print | sort)",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "typescript": "^5.8.3"
  }
}
